# =============================================================================
# Eval config for the oracle-trained DINOv2 + semantic-map checkpoint, with
# the semantic map constructed *online* at eval time (generalization test --
# the policy was trained on ground-truth maps, here it must use a map built
# from live depth + semantic).  Source'd by
# scripts/eval_il_mp3d_1scene_full.sh.
#
# Differs from eval_dinov2_oracle_semmap.env only in EXTRA_OPTS: with
# CACHE_ROOT unset, SemanticMapSensor falls back to its online branch
# (depth/semantic must therefore stay in AGENT_0.SENSORS, which is the
# default in the underlying objectnav_mp3d_semmap.yaml that the eval-side
# experiment yaml is based on).
# =============================================================================

: "${CONFIG:=configs/experiments/il_objectnav_mp3d_dinov2_semmap.yaml}"

# Same oracle-trained checkpoint as the cached-mode eval -- comparing apples
# to apples, only the map source at eval time differs.
: "${EVAL_CKPT:=data/new_checkpoints_dinov2_oracle_semmap/objectnav_il/mp3d_1scene_6cat_dinov2_oracle_semmap/ckpt.10.pth}"

: "${EVAL_SPLITS:=train val}"
: "${TEST_EPISODE_COUNT:=-1}"
: "${NUM_ENVIRONMENTS:=1}"

: "${SUCCESS_DISTANCE:=1.0}"
: "${ALLOW_SLIDING:=True}"

: "${VIDEO_ENABLED:=true}"
: "${VIDEO_FAILED_ONLY:=false}"
: "${VIDEO_FPS:=10}"
: "${VIDEO_RENDER_TOP_DOWN:=true}"

: "${OUT_ROOT:=data/eval_out/dinov2_oracle_semmap_online_ckpt10}"

export CONFIG EVAL_CKPT EVAL_SPLITS TEST_EPISODE_COUNT NUM_ENVIRONMENTS \
       VIDEO_ENABLED VIDEO_FAILED_ONLY VIDEO_FPS VIDEO_RENDER_TOP_DOWN OUT_ROOT \
       SUCCESS_DISTANCE ALLOW_SLIDING
